PORT=4100
ConnectionStrings__Default=
Jwt__AccessTokenSecret=
Frontend__BaseUrl=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

MinIOSettings__Endpoint=
MinIOSettings__AccessKey=
MinIOSettings__SecretKey=
MinIOSettings__BucketName=real-estate-assets
MinIOSettings__UseSSL=false
MinIOSettings__PublicUrl=

# Branding, currency, email, SMS, WhatsApp, AI and payment configuration
# are read from the existing agency_settings and agency_integration_settings tables.
